piVersion: v1
kind: PersistentVolume
metadata:
  name: app-pv
  labels:
    app: storage
spec:
  storageClassName: manual
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/app/data"
